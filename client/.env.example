# Base de la API REST del backend (server/).
VITE_API_URL=http://localhost:4000

# true => sirve las lecturas con MSW (sin backend). false => API real.
VITE_USE_MOCK=true

# Nota: FENRIR_FACTORY_ADDRESS y SEPOLIA_CHAIN_ID se toman del .env raiz del repo
# y se re-exponen al frontend automaticamente via vite.config.ts (define).
# No hace falta duplicarlas aqui.
